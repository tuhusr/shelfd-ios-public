/* =============================================================================
   30-add-password-provider.js
   -----------------------------------------------------------------------------
   Retired in v11.021.

   Google sign-in remains supported, so the previous "Set a password to keep
   your account" migration prompt/card is intentionally disabled. This file is
   kept as a harmless no-op so stale script references cannot recreate the
   modal.
   ========================================================================== */
(function() {
  'use strict';
})();
