node_modules/
.dev.vars
.env
*.log
.wrangler/
dist/

# Local-only working dirs — never track these.
# v10.391: untracked the historical version snapshots and recorded video
# clips so the repo stops bloating with multi-GB local archives.
z VERSIONS/
zz VERSIONS/
video examples/
versions/
shutdown prompts/
virtual mac/
testflight_feedback*.zip

